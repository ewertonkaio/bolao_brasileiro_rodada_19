[build]
  command = "bash build.sh"
  publish = "."
  functions = "netlify/functions"

[build.environment]
  # define o Node do build; as funcoes herdam essa mesma versao (fetch nativo)
  NODE_VERSION = "20"
